import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ ok: true, note: "stub — not yet implemented" });
}

export async function PATCH() {
  return NextResponse.json({ ok: true, note: "stub — not yet implemented" });
}
