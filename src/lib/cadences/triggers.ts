// Cadence enrollment triggers (stage_entered, manual, lead_created).
// Filled in during step 7 (nurture engine).

export async function maybeEnrollOnStageChange(_leadId: string, _newStageId: string): Promise<void> {
  // no-op stub
}
